# Third-Party Components Planned for the Implementation

This PRD bundle does not redistribute curl source. The implementation is designed to vendor a verified curl release and must include curl's COPYING file and notices. Planned Rust dependencies and their licenses must be generated from the final lockfile before release.
