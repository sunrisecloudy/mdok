# Vendored curl placeholder

Run `scripts/fetch-curl.sh` after filling the verified SHA-256 in `vendor/curl.sha256`.
Do not commit an unverified archive or generated source tree. Preserve curl's COPYING file and notices.
