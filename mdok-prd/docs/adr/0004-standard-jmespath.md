# ADR 0004: Strict Standard JMESPath

Status: Accepted

All checks and captures use standard JMESPath. Version 1 does not add custom functions. Specialized schema/regex/date checks may be separate block types later rather than dialect changes.
